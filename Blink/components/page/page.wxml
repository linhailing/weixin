<!--components/page/page.wxml-->
<view class="p">
    <image class="icon" src="{{left ? leftSrc : leftSrcDis }}" bind:tap="onClickLeft" />
    <text class="title">{{title}}</text>
    <image class="icon" src="{{right ? rightSrc : rightSrcDis }}" bind:tap="onClickRight" />
</view>
